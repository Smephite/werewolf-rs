version: "3.9"
services:
    server:
        image: werewolfrs/server
        build: 
            context: .
            dockerfile: ./Dockerfile_Server
    client:
        image: werewolfrs/client
        build:
            context: .
            dockerfile: ./Dockerfile_Client